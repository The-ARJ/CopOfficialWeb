/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // output: "export",
  reactStrictMode: true,
  basePath: "",
  assetPrefix: "",
  images: {
    domains: ['localhost'],
  },
};

module.exports = nextConfig;
